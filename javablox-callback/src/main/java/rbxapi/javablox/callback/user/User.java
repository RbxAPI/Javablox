package rbxapi.javablox.callback.user;

public class User implements rbxapi.javablox.api.user.User{

    /**
     * TODO: Define a function that logs in a User, returning AuthUser
     * if logs in successfully, passes Cookie to the returning AuthUser
     */
}
